### Poetry init

```bash
poetry init # init poetry prexisting project

poetry env activate # activating environment

```


**after then we can all we need packages**


```bash
poetry add django, djangorestframework

poetry run python manage.py runserver

```